package com.ycyw.domain;

/**
 * Rôle d’un utilisateur dans l’application.
 *
 * - SUPPORT : c’est un membre du support (il aide les utilisateurs).
 * - USER : c’est un utilisateur normal.
 */
public enum Role {
    SUPPORT,
    USER,
    AGENT
}
